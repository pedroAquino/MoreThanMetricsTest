// @flow
import * as React from 'react';
import injectSheet from "react-jss";
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';

type Props = {
  
};

const styles = theme => ({});

export function EditionBar(props: Props) {
  return (
    <div>
      
    </div>
  );
};

export default injectSheet(styles)(EditionBar);
